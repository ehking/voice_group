- اگر ffmpeg نصب نیست: `sudo apt install ffmpeg`
- در صورت خطای پورت 8000، مقدار `--port` در `run_dev.sh` را عوض کنید.
- برای پاک‌سازی صف و نتایج: پوشه `storage/` را خالی کنید و مجدداً `make dev` را اجرا کنید.
